# ===================
# PostgreSQL (shared)
# ===================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres-admin-password-change-me

# Second Brain DB credentials (used in init-db.sql)
SECONDBRAIN_DB_PASSWORD=secondbrain-password-change-me

# Dendrite DB credentials (used in init-db.sql and dendrite.yaml)
DENDRITE_DB_PASSWORD=dendrite-password-change-me

# ===================
# Dendrite Matrix Server
# ===================
# Your Matrix domain (e.g., matrix.yourdomain.com or just localhost for local use)
MATRIX_SERVER_NAME=localhost

# ===================
# Matrix Bot Configuration
# ===================
# For local Dendrite, use the internal Docker hostname
# MATRIX_HOMESERVER is set in docker-compose.yml to http://dendrite:8008

# Bot user credentials (create this user in Dendrite after setup)
MATRIX_USER_ID=@secondbrain:localhost
MATRIX_PASSWORD=bot-password-here
MATRIX_INBOX_ROOM=#sb-inbox:localhost

# Your user (where digests are sent)
DIGEST_TARGET_USER=@carlos:localhost

# ===================
# LLM APIs
# ===================
# GLM-4 (for classification - cheap model)
GLM_API_URL=https://api.z.ai/v1/chat/completions
GLM_API_KEY=your-glm-key
GLM_MODEL=glm-4

# Claude (for summaries and retrieval)
CLAUDE_API_URL=https://api.anthropic.com/v1/messages
CLAUDE_API_KEY=your-claude-key
CLAUDE_MODEL=claude-sonnet-4-20250514

# ===================
# Settings
# ===================
CONFIDENCE_THRESHOLD=0.6
